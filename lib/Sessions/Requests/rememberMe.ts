

const rememberMe = (): string => {
    // make some token // this is for test.
    let key = `${Math.random()*10 + " - rememberMe"}`
    if (true && false) 
    {
        return key;
    }
    return  key;
}

export {rememberMe};